---
title: "Pricing"
template: splash
prev: false
next: false
description: "ZeptoDB pricing — free Community usage, GitHub-first support, and transparent feature gates"
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid>
  <Card title="Community" icon="open-book">
    **Free — no license key required**

    Production-ready for single-node time-series workloads and Agent Memory pilots.

    - Core engine: ingestion, query, ASOF JOIN, LLVM JIT
    - Agent Memory: memories, context retrieval, exact/semantic cache, local stats
    - Client-supplied embedding storage and ranking
    - Python zero-copy, Python memory/cache helpers, C++ API, HTTP API, Arrow Flight
    - SQL, Arrow IPC ingest, MessagePack ingest, FIX / ITCH / Binance feed handlers
    - Parquet HDB on S3, GCS, or local NFS
    - Agent Memory sidecar snapshots, replay, snapshot metrics, and ANN footprint metrics
    - Basic RBAC, TLS 1.3, API key auth, rate limiting
    - Single-node deployment
    - Community support through GitHub Issues and Discussions

    [Get started →](/getting-started/quick_start/)
  </Card>
  <Card title="GitHub-first support" icon="approve-check-circle">
    **Community support through GitHub**

    ZeptoDB is keeping public adoption on GitHub for now. Use the repository for source, issues, discussions, and roadmap feedback.

    - Source and releases: [github.com/zeptodb/zeptodb](https://github.com/zeptodb/zeptodb)
    - Bug reports and feature requests: [GitHub Issues](https://github.com/zeptodb/zeptodb/issues)
    - Architecture and usage questions: [GitHub Discussions](https://github.com/zeptodb/zeptodb/discussions)
    - Community chat: [Discord](https://discord.gg/PAtzvCa7)

    [Open GitHub →](https://github.com/zeptodb/zeptodb)
  </Card>
</CardGrid>

---

## Feature gates

Some capabilities are still license-gated in the codebase and docs. This page lists them for transparency, but the website does not currently route readers into a procurement or meeting flow.

- Multi-node time-series clustering with automatic sharding
- SSO / OIDC integration (Okta, Auth0, Azure AD, Google Workspace)
- Audit log export for SIEM integration
- Advanced RBAC with per-tenant namespace isolation
- Kafka, AWS Kinesis, ROS 2, OPC-UA, MQTT, and Telegraf deployment support
- Migration tools (kdb+ / ClickHouse / DuckDB / TimescaleDB)
- Geo-replication for time-series deployments
- Rolling upgrades with zero downtime
- Multi-node Agent Memory guidance for routed writes, fan-out search/context, cluster stats, replica WAL policy, and owner-failover reporting
- Long-term support releases

---

## Local feature-gate trial

You can generate a local 30-day trial key from an admin endpoint:

```bash
# Generate a trial license (single-node, all gated features enabled)
curl -X POST http://localhost:8123/admin/license/trial \
  -H "Authorization: Bearer $ADMIN_KEY"
```

Trial keys are single-node (`max_nodes=1`) with all gated features enabled. After 30 days, the instance gracefully downgrades to Community edition.

---

## FAQ

**Is ZeptoDB Community really free?**
Yes. Community includes the full single-node time-series core, SQL, HTTP API, Python DSL, Arrow Flight, Parquet HDB, and Agent Memory for local pilots and single-node production systems.

**What is the support model right now?**
GitHub-first. Use [Issues](https://github.com/zeptodb/zeptodb/issues) for bugs and feature requests, and [Discussions](https://github.com/zeptodb/zeptodb/discussions) for architecture questions or roadmap feedback.

**What requires a license gate?**
The gated surfaces are:

- Multi-node time-series clustering with automatic sharding
- SSO / OIDC integration (Okta, Auth0, Azure AD, Google Workspace)
- Audit log export for SIEM integration
- Advanced RBAC with per-tenant namespace isolation
- Kafka, AWS Kinesis, ROS 2, OPC-UA, MQTT, and Telegraf deployment support
- Migration tools (kdb+ / ClickHouse / DuckDB / TimescaleDB)
- Geo-replication for time-series deployments
- Rolling upgrades with zero downtime
- Multi-node Agent Memory guidance for routed writes, fan-out search/context, cluster stats, replica WAL policy, and owner-failover reporting
- Long-term support releases

**Is Agent Memory cluster-wide today?**
Agent Memory has a routed multi-node operating path for writes, point reads, fan-out memory search/context, semantic-cache fan-out, owner-local persistence, replica WAL durability policy, cluster-scoped stats, and owner-failover reporting. Shard migration dual-write/catch-up remains future work.

**Does ZeptoDB call LLM or embedding providers?**
No. Applications supply embeddings and call model providers. ZeptoDB stores memory records, ranks candidates, assembles context, and performs exact/semantic cache lookup.

**How does license validation work?**
Fully offline — RS256 signature verification with no phone-home. Load via environment variable (`ZEPTODB_LICENSE_KEY`), file (`/etc/zeptodb/license.key`), or HTTP API (`POST /admin/license`).

**What happens when a license expires?**
You get a warning before expiry, then a grace period where the deployment continues working. After the grace period, the instance downgrades to Community edition. No data is locked.

**Do you offer managed cloud hosting?**
Not right now. For self-hosting, see [Production Deployment](/deployment/production_deployment/) and [Kubernetes Operations](/operations/kubernetes_operations/). For roadmap questions, use [GitHub Discussions](https://github.com/zeptodb/zeptodb/discussions).
